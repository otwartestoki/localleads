"use client";import { site as rawSite } from '@/config/site';import { media } from '@/config/media';import { useState } from 'react';import Reveal from '../ui/Reveal';import { radiusClass, sectionBg } from '@/lib/style';
const site:any = rawSite;
export default function BeforeAfter(){const[pos,setPos]=useState(50);return <section id="before-after" className={`section ${sectionBg('beforeAfter')}`}><div className="container grid md:grid-cols-[.8fr_1.2fr] gap-10 items-center"><Reveal><p className="text-sm font-black uppercase tracking-[.2em]" style={{color:site.colors.primary}}>Przed / po</p><h2 className="mt-3 text-4xl md:text-5xl font-black tracking-[-.04em]">{site.beforeAfter.title}</h2><p className="mt-4 text-lg leading-8 opacity-75">{site.beforeAfter.text}</p></Reveal><Reveal from="right"><div className={`${radiusClass()} card p-3`}><div className={`relative aspect-[16/10] overflow-hidden ${radiusClass()} bg-slate-200 select-none`}><img src={media.images.before} alt="Przed" className="absolute inset-0 h-full w-full object-cover"/><img src={media.images.after} alt="Po" className="absolute inset-0 h-full w-full object-cover" style={{clipPath:`inset(0 ${100-pos}% 0 0)`}}/><div className="absolute top-0 bottom-0 w-1 bg-white shadow-xl" style={{left:`${pos}%`}}><div className="absolute top-1/2 -translate-y-1/2 -left-6 h-12 w-12 rounded-full bg-white shadow-xl grid place-items-center font-black text-slate-800">↔</div></div><input aria-label="before after slider" className="absolute inset-0 opacity-0 cursor-ew-resize w-full h-full" type="range" min="0" max="100" value={pos} onChange={e=>setPos(+e.target.value)}/><span className="absolute left-4 top-4 rounded-full bg-black/55 text-white px-3 py-1 text-xs font-bold">PRZED</span><span className="absolute right-4 top-4 rounded-full bg-black/55 text-white px-3 py-1 text-xs font-bold">PO</span></div></div></Reveal></div></section>}
