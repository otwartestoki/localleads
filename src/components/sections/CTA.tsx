import { site } from '@/config/site';
import { getButtonClass } from '@/lib/uiStyles';
import { contactHref } from '@/lib/contact';
export default function CTA(){return <section id="cta" className="py-16 brand-section-soft"><div className="container"><div className="rounded-3xl border border-[var(--brand-primary)]/25 bg-gradient-to-br from-[rgba(56,189,248,.16)] via-[rgba(30,64,175,.16)] to-[rgba(139,92,246,.16)] p-8 text-white shadow-[0_28px_90px_rgba(0,0,0,.28)] md:p-12"><div className="grid gap-6 md:grid-cols-[1fr_auto] md:items-center"><div><p className="text-sm font-black uppercase tracking-[.22em] text-[var(--brand-primary-soft)]">Szybki start prospectingu</p><h2 className="mt-3 text-3xl font-black tracking-[-.04em] md:text-5xl">Zamów paczkę leadów pod swoją branżę lub region</h2><p className="mt-4 max-w-2xl text-[var(--brand-text)]">Napisz, kogo szukasz: branża, miasto lub województwo. Przygotuję propozycję pakietu, zakresu i ceny.</p></div><a className={getButtonClass({tone:'primary',className:'whitespace-nowrap'})} href={contactHref({ source: 'sekcja CTA', topic: 'paczka leadów pod branżę lub region' })}>{site.contact.cta}</a></div></div></div></section>}
